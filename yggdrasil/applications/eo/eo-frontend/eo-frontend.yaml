eo-base-helm-chart:
  ingress:
    hosts:
      - {{ .Values.ingressDomain }}
    httpMiddelwares:
      - default-https-only@kubernetescrd
    tlsSecretName: elop-cert
    pathExceptions:
      - /api
      - /auth
      - /admin

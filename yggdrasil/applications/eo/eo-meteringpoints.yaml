eo-base-helm-chart:
  deployments:
    - name: auth-api
      image:
        tag: "latest"
    - name: auth-consumer
      image:
        tag: "latest"
  ingress:
    hosts:
      - {{ .Values.ingressDomain }}

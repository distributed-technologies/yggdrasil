cadvisor:
  monitor:
    enabled: true
    additionalLabels:
      instance: primary

kube-state-metrics:
  enabled: true
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        instance: primary

prometheus-node-exporter:
  enabled: true
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        instance: primary

prometheus-kafka-exporter:
  enabled: false

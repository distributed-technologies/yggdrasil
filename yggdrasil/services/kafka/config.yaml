name: kafka
clusterService: true
namespace: kafka
description: Kafka.

project:
  name: kafka
  server: https://kubernetes.default.svc
  sourceRepos:
    - '*'

apps:
  - name: kafka
    replace: true
    source:
      repoURL: "https://kahyt.azurecr.io/test-repo/"
      targetRevision: v0.1.0
      path: chart
      valuesFile: "kafka.yaml"
      enableOCI: true
